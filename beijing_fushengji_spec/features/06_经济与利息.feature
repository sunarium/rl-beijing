# language: zh-CN
# 来源: SelectionDlg.cpp HandleCashAndDebt / 04 所在日序
@backend @rule-engine
功能: 经济与利息
  债务每天 +10%，银行每天 +1%。两者都使用 C double 运算后再以 `(long)` 向零截断（整数化）。
  **跨语言注意**：为保证逐位一致，后端应采用与 C IEEE-754 double 相同的运算，或采用下述已固化的等价示例值；不得使用整数比例公式（会与截断结果漂移）。

  背景:
    假如 日常序列执行到利息结算步骤（价格生成之后、商业事件之前）
    # 来源: SelectionDlg.cpp:1459

  # ===== 每日利息 =====

  @verified
  场景: 每日债务利息
    # 来源: SelectionDlg.cpp:1413
    当 结算一次利息
    那么 债务 = 债务 + (long)(债务 × 0.10)
    而且 (long) 为向零截断的整数化

  @verified
  场景: 每日存款利息
    # 来源: SelectionDlg.cpp:1417
    当 结算一次利息
    那么 存款 = 存款 + (long)(存款 × 0.01)
    而且 (long) 为向零截断的整数化

  @verified
  场景大纲: 债务利息示例（截断语义）
    # 来源: SelectionDlg.cpp:1413（C double 逐位核对）
    假如 债务为 <前值>
    当 结算一次利息
    那么 债务变为 <后值>

    例子:
      | 前值 | 后值 |
      | 5000 | 5500 |
      | 5500 | 6050 |
      | 6050 | 6655 |
      | 6655 | 7320 |
      | 7320 | 8052 |
      | 8052 | 8857 |

  @verified
  场景大纲: 存款利息示例（截断语义）
    # 来源: SelectionDlg.cpp:1417（C double 逐位核对）
    假如 存款为 <前值>
    当 结算一次利息
    那么 存款变为 <后值>

    例子:
      | 前值 | 后值 |
      | 1000 | 1010 |
      | 1010 | 1020 |
      | 1020 | 1030 |
      | 1030 | 1040 |
      | 1040 | 1050 |
      | 1050 | 1060 |

  # ===== 时机 =====

  @verified
  场景: 利息处理时机
    # 来源: HandleNormalEvents 顺序
    那么 每个移动日结算利息恰好一次
    而且 顺序为价格生成之后、商业事件之前
    而且 游戏启动初始化时也调用一次 HandleCashAndDebt（同样结算首次利息）
