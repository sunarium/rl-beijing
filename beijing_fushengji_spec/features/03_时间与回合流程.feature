# language: zh-CN
# 来源: SelectionDlg.cpp HandleNormalEvents
@backend @state
功能: 时间与回合流程
  本游戏没有独立的"回合"概念：**每次移动到新地点 = 1 天**。每一天按固定的顺序依次处理价格、利息、商业事件、健康事件、偷钱事件、讨债与天数递减。
  该"每日固定处理顺序"是实现逐位重放的关键，必须严格保持与 C++ 一致。

  背景:
    假如 玩家移动到新地点
    # 来源: SelectionDlg.cpp:1447

  # ===== 每日主循环顺序 =====

  @verified
  场景: 一次移动的完整处理顺序
    # 来源: SelectionDlg.cpp:1447-1490
    当 移动到新地点
    那么 按以下固定顺序依次处理，且不得调换:
      | 步骤 | 处理 | 引用 |
      | ① | 重新生成黑市价格（makeDrugPrices: 正常 3 剔除 / 最后2天 0 剔除） | 1452-1457 |
      | ② | 结算利息（债务+10%、存款+1%） | 1459, HandleCashAndDebt |
      | ③ | 触发商业事件（DoRandomStuff） | 1461 |
      | ④ | 刷新黑市列表（DisplayDrugs） | 1463 |
      | ⑤ | 触发健康事件（DoRandomEvent） | 1465 |
      | ⑥ | 触发偷钱与黑客事件（OnSteal） | 1466 |
      | ⑦ | 若债务>100000 则健康-30 并弹窗 | 1468-1478 |
      | ⑧ | 天数 m_nTimeLeft 减 1 | 1481 |
      | ⑨ | 更新窗口标题、天数=1 时警告、天数=0 时结算结束 | 1483-1543 |

  # ===== 天数递减与结束 =====

  @verified
  场景: 天数从 40 递减到 0
    # 来源: SelectionDlg.cpp:1480-1482
    假如 初始 m_nTimeLeft=40
    那么 每移动到新地点 m_nTimeLeft 减 1
    而且 窗口标题为「北京浮生(已完成天数/40天)」

  @verified
  场景: 剩余 1 天时提示清仓
    # 来源: SelectionDlg.cpp:1485-1490
    假如 m_nTimeLeft 等于 1
    当 移动到新地点
    那么 弹日记「俺明天回家乡，快把全部货物卖掉。」
    而且 游戏继续

  @bug-faithful @verified
  场景: 剩余 0 天时强制清仓并结算
    # 来源: SelectionDlg.cpp:1492-1543
    # 原版缺陷: 若玩家刻意不清仓，剩余货物按"当前黑市价"自动卖出（可能因最后一天价格已刷新而低价清仓）。
    假如 m_nTimeLeft 变为 0
    当 日常序列推进
    那么 弹日记「俺已经在北京40天了，该回去结婚去了。」
    而且 若仓库仍有货物，前列名称为「系统替我卖了剩余货物: <名称,>。」
    而且 按当前黑市价强制卖出全部剩余货物（MyCash += 数量×当前价）
    而且 随后调用结算 OnExit（见 12_得分与排行）

  # ===== 讨债 =====

  @verified
  场景: 债务超过十万触发讨债
    # 来源: SelectionDlg.cpp:1468-1478
    假如 我的债务大于 100000
    当 移动到新地点
    那么 弹日记「俺欠钱太多，村长叫一群老乡揍了俺一顿!」
    而且 健康减 30
    而且 播放音效 kill.wav（若声音开启）

  @verified
  场景: 讨债后健康可为负并致死
    # 来源: SelectionDlg.cpp:1629-1635
    假如 讨债将健康减到小于 0
    那么 进入死亡结局流程（见 08_健康事件）
